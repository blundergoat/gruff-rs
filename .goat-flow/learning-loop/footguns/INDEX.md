---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Cross-File Dead-Code Signal Breaks Under Partial Discovery](analyzer.md) (search: "## Footgun: Cross-File Dead-Code Signal Breaks Under Partial Discovery") - `src/analyse_project/dead_code.rs` (search: `analyse_project_dead_code_rules`) decides whether a private item is referenced by asking `ProjectContext` for the discovered identifier count.
- [Enriched Rule Definitions Require A `related` Arm](analyzer.md) (search: "## Footgun: Enriched Rule Definitions Require A `related` Arm") - `src/rules/mod.rs` (search: `macro_rules! rule_definition`) has only two macro arms: a plain definition with no optional sections, and an enriched definition that requires both `false_positives:` and…
- [Bare-Bare Equality Closures Look Like `.contains()` But Often Aren't](analyzer.md) (search: "## Footgun: Bare-Bare Equality Closures Look Like `.contains()` But Often Aren't") - `src/built_in_rules/modernisation_rules.rs` (search: `fn analyse_manual_contains`) flags `iter().any(|x| x == y)` shapes that should use `.contains(&y)`.
- [Fixture Findings Are Intentional](analyzer.md) (search: "## Footgun: Fixture Findings Are Intentional") - `fixtures/sample.rs` (search: `let api_key =`) intentionally includes secret-looking strings, command execution, a long parameter list, and a weak test.
- [Code-Shape Rules Can Scan Fixture Strings](analyzer.md) (search: "## Footgun: Code-Shape Rules Can Scan Fixture Strings") - `src/parser/mod.rs` (search: `fn strip_rust_string_literals`) masks Rust string literals before code-shape checks such as complexity, unwrap, process command, unsafe, and test-quality scans.
- [Per-Rule Guards In `analyse_waste_line` Must Stay Symmetric](analyzer.md) (search: "## Footgun: Per-Rule Guards In `analyse_waste_line` Must Stay Symmetric") - `src/built_in_rules/behavior_rules.rs` (search: `fn analyse_waste_line`) hosts both `waste.unwrap-expect` and `waste.unnecessary-clone-candidate` as sibling `if`-blocks against the same `line`.
- [Same-Line Findings Can Dedupe Together](analyzer.md) (search: "## Footgun: Same-Line Findings Can Dedupe Together") - `src/report.rs` (search: `hasher.update(symbol.clone().unwrap_or_default().as_bytes())`) derives finding fingerprints from rule id, file path, line, and symbol.
- [Workflow Text Rules Need List-Item Syntax](analyzer.md) (search: "## Footgun: Workflow Text Rules Need List-Item Syntax") - `src/built_in_rules/github_metadata_rules.rs` (search: `fn analyse_ci_github_event_shell_interpolation`) scans GitHub Actions YAML as deterministic text, not with a YAML parser.
- [check-ignore Needs Hierarchical Gitignore Context](analyzer.md) (search: "## Footgun: check-ignore Needs Hierarchical Gitignore Context") - `src/check_ignore.rs` (search: `pub(crate) fn run_check_ignore`) is the hook-facing contract for "would gruff ignore this path?" The discovery walk gets hierarchical `.gitignore` handling from…
- [Dead-Code Reference Masking Must Preserve Structured Attribute References](analyzer.md) (search: "## Footgun: Dead-Code Reference Masking Must Preserve Structured Attribute References") - `src/parser/mod.rs` (search: `fn rust_code_reference_source`) masks arbitrary comments and strings before dead-code reference counting, then appends only structured references such as `serde(default…
- [Loop-Scoped Rules Must Mask Comments](analyzer.md) (search: "## Footgun: Loop-Scoped Rules Must Mask Comments") - `src/built_in_rules/perf_rules.rs` (search: `fn analyse_performance_block`) must feed comment-masked Rust text into `loop_pattern_count_filtered`.
- [Assertion Unwrap Exemptions Need Receiver Context](analyzer.md) (search: "## Footgun: Assertion Unwrap Exemptions Need Receiver Context") - `src/built_in_rules/test_rules.rs` (search: `fn body_contains_only_assertion_subject_unwraps`) exempts `test-quality.unwrap-in-test` only when every `.unwrap()` is inside an assertion macro and the…
- [Test-Quality Assertion Rules Must Mask Comments](analyzer.md) (search: "## Footgun: Test-Quality Assertion Rules Must Mask Comments") - `src/built_in_rules/blocks.rs` (search: `let searchable_body = strip_rust_string_literals(&block.body);`) passes a string-masked but comment-preserved body into `analyse_test_assertions`.
- [Regex Shadow Windows Miss Rust Binding Patterns](analyzer.md) (search: "## Footgun: Regex Shadow Windows Miss Rust Binding Patterns") - `src/built_in_rules/helpers.rs` (search: `fn literal_is_asserted_before_shadow`) treats shadowing as a textual `let name` occurrence.
- [Text-Pattern Rules Self-Fire On Their Own Sentinel Values](analyzer.md) (search: "## Footgun: Text-Pattern Rules Self-Fire On Their Own Sentinel Values") - Rules that scan raw source text for literal patterns (`sensitive-data.*`, `security.hardcoded-bind-all-interfaces`, `security.weak-crypto`, etc.) cannot distinguish "this byte sequence appears in…
- [Wrapper-Module Fan-Out Hits 8 When Adding New Rule Files](analyzer.md) (search: "## Footgun: Wrapper-Module Fan-Out Hits 8 When Adding New Rule Files") - `src/built_in_rules/rust_block_rules.rs` and `src/built_in_rules/rust_other_rules.rs` are wrapper modules that mount per-rule sub-files via `#[path = "..."] mod ...;`.
- [Threshold Tuning Ripples Through Calibration Matrix](calibration.md) (search: "## Footgun: Threshold Tuning Ripples Through Calibration Matrix") - Bumping any rule's numeric threshold without updating the corresponding calibration positive case silently breaks `rule_calibration_matrix_covers_every_rule` (search:…
- [Stripping `paths.ignore` Defaults From `.gruff-rs.yaml`](config.md) (search: "## Footgun: Stripping `paths.ignore` Defaults From `.gruff-rs.yaml`") - `.gruff-rs.yaml` (search: `paths:`) ships with discovery-time ignores for agent, CI, fixture, and vendor directories.
- [Default-Allowlist Constants Duplicate Between config.rs And init.rs](config.md) (search: "## Footgun: Default-Allowlist Constants Duplicate Between config.rs And init.rs") - `src/config.rs` (search: `pub(crate) const DEFAULT_ABBREVIATIONS`) holds the compiled-in default that `Config::default()` uses to populate `accepted_abbreviations`.
- [Rule Option Names Do Not Imply Their Semantics](config.md) (search: "## Footgun: Rule Option Names Do Not Imply Their Semantics") - A rule option's NAME is not enough to know what setting it does.
- [Required-Field Config Schema Bumps Break Every Test Fixture](config.md) (search: "## Footgun: Required-Field Config Schema Bumps Break Every Test Fixture") - 0.1.2 M08a introduced the required `schemaVersion: gruff-rs.config.v1` field in `src/config_loader/mod.rs` (search: `fn apply_config_value`, look for the `missing the required` error).
- [Interpreter Heredoc Bodies Bypass The Shell-Only Deny Guard](deny-dangerous.md) (search: "## Footgun: Interpreter Heredoc Bodies Bypass The Shell-Only Deny Guard") - A Codex Bash tool call can place a destructive shell, file, Git, or network mutation inside a Python, Node, Ruby, Perl, PHP, sed, awk, or database-client heredoc and receive no denial.
- [Preflight Dogfood Output Is Truncated To 20 Findings](preflight.md) (search: "## Footgun: Preflight Dogfood Output Is Truncated To 20 Findings") - `scripts/preflight-checks.sh` (search: `dogfood_source_scan` and `sed -n '1,20p'`) caps the dogfood failure list shown to the user at 20 lines.
- [Stale Target Binaries Can Invalidate CLI Proofs](preflight.md) (search: "## Footgun: Stale Target Binaries Can Invalidate CLI Proofs") - `target/debug/gruff-rs` and `target/release/gruff-rs` are local Cargo artifacts, not freshness-guaranteed project entrypoints.
- [Cargo Install Will Not Adopt An Unmanaged Existing Binary](preflight.md) (search: "## Footgun: Cargo Install Will Not Adopt An Unmanaged Existing Binary") - `cargo install <crate> --version <exact> --locked` can fail with `binary already exists in destination` when the destination already contains the right binary but Cargo's install metadata does not…
- [Release Archive Shape Is A Cross-File Contract](preflight.md) (search: "## Footgun: Release Archive Shape Is A Cross-File Contract") - `.github/workflows/release.yml` (search: `Stage archive and build identity`) invokes the production `stage-archive` contract.
- [Perf Harness Empty Patch Must Stay Parseable](preflight.md) (search: "## Footgun: Perf Harness Empty Patch Must Stay Parseable") - `scripts/test-performance.sh` includes a changed-region scenario named `src.diff-empty` (search: `add_scenario "src.diff-empty"`).
- [Per-Format Renderer Helpers Tend To Duplicate](report.md) (search: "## Footgun: Per-Format Renderer Helpers Tend To Duplicate") - `src/report.rs` (search: `pub(crate) fn pillar_label`) maps `Pillar` variants to canonical kebab-case strings used in JSON, HTML, Markdown, and text output.
- [AnalysisReport Tree Silently Reshapes gruff.analysis.v2 JSON](report.md) (search: "## Footgun: AnalysisReport Tree Silently Reshapes gruff.analysis.v2 JSON") - `src/report.rs` (search: `pub(crate) struct AnalysisReport`) and every type reachable from it (`Summary`, `Finding`, `ScoreReport`, `PillarScore`, `FileScore`, `RunInfo`, `BaselineReport`, etc.) are…
- [Digest Helpers That Pull From Registry Defaults Instead Of Findings](report.md) (search: "## Footgun: Digest Helpers That Pull From Registry Defaults Instead Of Findings") - When a digest helper (e.g.
- [HashMap Iteration Leaks Into Deterministic Outputs](report.md) (search: "## Footgun: HashMap Iteration Leaks Into Deterministic Outputs") - `Config` (search: `pub(crate) struct Config` in `src/config.rs`) stores `rule_settings: HashMap<String, RuleSetting>` and `string_array_options: HashMap<String, Vec<String>>`.
- [Mutation-Step Ordering Decides Whether Deltas Match The Final Report](report.md) (search: "## Footgun: Mutation-Step Ordering Decides Whether Deltas Match The Final Report") - `run_analysis_in_project` (search: `pub(crate) fn run_analysis_in_project` in `src/analysis.rs`) is a sequence of mutations on a `Vec<Finding>`.
- [Shared Analysis Core Must Stay Command-Neutral](report.md) (search: "## Footgun: Shared Analysis Core Must Stay Command-Neutral") - `src/analysis.rs` (search: `pub(crate) fn run_analysis_in_project`) feeds `analyse`, `report`, `summary`, dashboard scans, and tests.
- [Candidate Security Rules Must Recognise Idiomatic Defence Patterns](security-analysis.md) (search: "## Footgun: Candidate Security Rules Must Recognise Idiomatic Defence Patterns") - `src/built_in_rules/path_traversal_rules.rs` (search: `fn analyse_path_traversal_candidate`) flags filesystem path construction from non-literal identifiers.
- [Secret-Key Case Sensitivity Depends On File Kind](security-analysis.md) (search: "## Footgun: Secret-Key Case Sensitivity Depends On File Kind") - `src/built_in_rules/secret_rules.rs` (search: `fn config_like_secret_regex`) intentionally allows lowercase secret-like keys only for structured config formats such as YAML, JSON, TOML, `.env`, and…
- [Process Command Needs Risk Signals](security-analysis.md) (search: "## Footgun: Process Command Needs Risk Signals") - `src/built_in_rules/behavior_rules.rs` (search: `fn analyse_process_commands`) reports `security.process-command` only when `process_command_risk_signals` finds a concrete risk shape such as shell…
- [Candidate Taint Rules Can Taint Their Own Predicate Booleans](security-analysis.md) (search: "## Footgun: Candidate Taint Rules Can Taint Their Own Predicate Booleans") - `src/built_in_rules/network_security_rules.rs` (search: `fn analyse_template_injection_xss`) uses a bounded same-function taint model.
- [A Rule Exemption That Counts Only Named Placeholders Hides Positional Injection](security-analysis.md) (search: "## Footgun: A Rule Exemption That Counts Only Named Placeholders Hides Positional Injection") - `src/built_in_rules/behavior_rules/tls_sql.rs` (search: `fn fixed_placeholder_arity_is_safe`) exempts `security.sql-dynamic-query` for the safe IN-clause idiom: a `let placeholders = repeat_n("?",…
- [High-Entropy Inert Skip Was Tuned To One Model-ID Shape, Not A Safe Principle](security-analysis.md) (search: "## Footgun: High-Entropy Inert Skip Was Tuned To One Model-ID Shape, Not A Safe Principle") - `src/built_in_rules/helpers.rs` (search: `fn is_structured_high_entropy_non_secret`) skips inert high-entropy strings so `sensitive-data.high-entropy-string` (error severity) does not fire on base64…
- [Text Proof/Evidence Helpers Match Names Too Loosely](security-analysis.md) (search: "## Footgun: Text Proof/Evidence Helpers Match Names Too Loosely") - Several rules "prove" a value safe, or find "evidence" it is risky, by scanning nearby source text for a binding or function.
- [Clearing Git Env Vars Does Not Fully Neutralise The Subprocess](security-analysis.md) (search: "## Footgun: Clearing Git Env Vars Does Not Fully Neutralise The Subprocess") - `src/changed_region.rs` (search: `fn git_command`) hardens the diff subprocess by removing `GIT_EXTERNAL_DIFF` and pointing config/hooks at `/dev/null`.
