# Git Commit Guidance

Use concise conventional commits when a commit is requested, for example `feat: add baseline filtering` or `fix: preserve dashboard cwd`. Keep unrelated changes out of the same commit.

Do not commit generated Cargo output, local IDE state, analyzer baseline/history files, or local goat-flow session logs.
